<Window x:Class="Friday.AddCommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Command" Height="460" Width="850"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown">

    <Window.Resources>
        <FontFamily x:Key="BrandFont">pack://FRIDAY//Assets//IntroFriday.ttf</FontFamily>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0A1A11"/>
            <Setter Property="Foreground" Value="#C8F0DC"/>
            <Setter Property="BorderBrush" Value="#1A3D28"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Gill Sans MT"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#16A34A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#0E2018"/>
            <Setter Property="Foreground" Value="#C8F0DC"/>
            <Setter Property="BorderBrush" Value="#1A3D28"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Gill Sans MT"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#142E1E"/>
                                <Setter TargetName="b" Property="BorderBrush" Value="#16A34A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#5BA07A"/>
            <Setter Property="FontFamily" Value="Gill Sans MT"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="2,0,0,6"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="#091510" BorderBrush="#1A3D28" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="40" ShadowDepth="10" Opacity="0.7"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="20,0,14,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="FRIDAY" FontFamily="{StaticResource BrandFont}" FontSize="18" Margin="0" Foreground="#5BA07A"/>
                    <TextBlock Text="  ·  КОМАНДА" FontFamily="Gill Sans MT" FontSize="11" Foreground="#2D5A40" VerticalAlignment="Center" Margin="0,2,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="✓ ГОТОВО" Background="#16A34A" BorderBrush="#16A34A" Height="30" Padding="15,0" Margin="0,0,10,0" Click="AddButton_Click"/>
                    <Button Content="✕" Click="Close_Click" Width="30" Height="30" Background="Transparent" BorderThickness="0" Foreground="#2D5A40"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="#0E2018" CornerRadius="0,0,16,16" BorderBrush="#1A3D28" BorderThickness="0,1,0,0" Padding="30,25">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,25,0">
                        <TextBlock Text="{Binding WindowTitle}" FontFamily="{StaticResource BrandFont}" FontSize="24" Foreground="#C8F0DC" Margin="0,0,0,20"/>

                        <TextBlock Text="Имя команды (триггер)"/>
                        <TextBox x:Name="CommandNameTextBox" Margin="0,0,0,15" PreviewTextInput="CommandNameTextBox_PreviewTextInput"/>

                        <TextBlock Text="Описание"/>
                        <TextBox x:Name="DescriptionTextBox" Margin="0,0,0,20"/>

                        <CheckBox x:Name="SetPasswordCheckBox" Content="Требовать пароль при запуске" Foreground="#C8F0DC" FontFamily="Gill Sans MT" FontSize="13"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="25,0,0,0">
                        <TextBlock Text="Список действий"/>
                        <Button Content="+ ДОБАВИТЬ ДЕЙСТВИЕ" Click="AddActionButton_Click" Height="36" Margin="0,0,0,15"/>

                        <ScrollViewer VerticalScrollBarVisibility="Auto" Height="210">
                            <ItemsControl x:Name="ActionsItemsControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#0A1A11" BorderBrush="#1A3D28" BorderThickness="1" CornerRadius="6" Margin="0,0,0,6" Padding="10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding}" Foreground="#C8F0DC" FontFamily="Gill Sans MT" FontSize="13" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0"/>
                                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                    <Button Content="✎" Click="EditButton_Click" Width="28" Height="28" Margin="0,0,4,0"/>
                                                    <Button Content="✕" Click="DeleteButton_Click" Width="28" Height="28" Background="#3F1515" BorderBrush="#EF4444" Foreground="#FECACA"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>