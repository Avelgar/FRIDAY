<Window x:Class="Friday.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход" Height="400" Width="360"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown">

    <Window.Resources>
        <FontFamily x:Key="BrandFont">pack://FRIDAY//Assets//IntroFriday.ttf</FontFamily>

        <Style TargetType="Hyperlink">
            <Setter Property="Foreground" Value="#5BA07A"/>
            <Setter Property="TextDecorations" Value="None"/>
            <Setter Property="FontFamily" Value="Gill Sans MT"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#16A34A"/>
                    <Setter Property="TextDecorations" Value="Underline"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0A1A11"/>
            <Setter Property="Foreground" Value="#C8F0DC"/>
            <Setter Property="BorderBrush" Value="#1A3D28"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Gill Sans MT"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#16A34A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ИСПРАВЛЕННЫЙ СТИЛЬ PASSWORDBOX (БЕЗ BasedOn) -->
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#0A1A11"/>
            <Setter Property="Foreground" Value="#C8F0DC"/>
            <Setter Property="BorderBrush" Value="#1A3D28"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Gill Sans MT"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Root" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#16A34A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="#091510" BorderBrush="#1A3D28" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="40" ShadowDepth="10" Opacity="0.7"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="20,0,14,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="FRIDAY" FontFamily="{StaticResource BrandFont}" FontSize="18" Foreground="#5BA07A"/>
                    <TextBlock Text="  ·  АВТОРИЗАЦИЯ" FontFamily="Gill Sans MT" FontSize="11" Foreground="#2D5A40" VerticalAlignment="Center" Margin="0,2,0,0"/>
                </StackPanel>
                <Button Content="✕" Click="Close_Click" Width="30" Height="30" Background="Transparent" BorderThickness="0" Foreground="#2D5A40" HorizontalAlignment="Right" Cursor="Hand"/>
            </Grid>

            <Border Grid.Row="1" Background="#0E2018" CornerRadius="0,0,16,16" BorderBrush="#1A3D28" BorderThickness="0,1,0,0" Padding="5">
                <StackPanel>
                    <TextBlock Text="С возвращением" FontFamily="{StaticResource BrandFont}" FontSize="24" Foreground="#C8F0DC" Margin="0,0,0,25" HorizontalAlignment="Center"/>

                    <TextBlock Text="Email или логин" FontFamily="Gill Sans MT" FontSize="12" FontWeight="Bold" Foreground="#5BA07A" Margin="2,0,0,6"/>
                    <TextBox x:Name="LoginTextBox" Margin="0,0,0,15"/>

                    <TextBlock Text="Пароль" FontFamily="Gill Sans MT" FontSize="12" FontWeight="Bold" Foreground="#5BA07A" Margin="2,0,0,6"/>
                    <PasswordBox x:Name="PasswordBox" Margin="0,0,0,25"/>

                    <Button Content="ВОЙТИ В СИСТЕМУ" Click="LoginButton_Click" Background="#16A34A" Foreground="#D1FAE5" BorderThickness="0" FontFamily="Gill Sans MT" FontWeight="Bold" FontSize="13" Height="42" Cursor="Hand" Margin="0,0,0,15">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <TextBlock HorizontalAlignment="Center">
                        <Hyperlink x:Name="ForgotPasswordLink" Click="ForgotPasswordLink_Click">Забыли пароль?</Hyperlink>
                    </TextBlock>

                    <TextBlock x:Name="ErrorTextBlock" Foreground="#EF4444" FontFamily="Gill Sans MT" FontSize="12" Margin="0,5,0,0" TextWrapping="Wrap" Visibility="Collapsed" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>